---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: vultrbaremetalmachines.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: VultrBareMetalMachine
    listKind: VultrBareMetalMachineList
    plural: vultrbaremetalmachines
    shortNames:
    - vbm
    singular: vultrbaremetalmachine
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Cluster to which this VultrBareMetalMachine belongs
      jsonPath: .metadata.labels.cluster\.x-k8s\.io/cluster-name
      name: Cluster
      type: string
    - description: Vultr bare metal server state
      jsonPath: .status.subscriptionStatus
      name: State
      type: string
    - description: Machine ready status
      jsonPath: .status.ready
      name: Ready
      type: boolean
    - description: Vultr bare metal plan
      jsonPath: .spec.planID
      name: Plan
      type: string
    - description: Vultr bare metal server ID
      jsonPath: .spec.providerID
      name: ServerID
      type: string
    - description: Machine object which owns this VultrBareMetalMachine
      jsonPath: .metadata.ownerReferences[?(@.kind=="Machine")].name
      name: Machine
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta2
    schema:
      openAPIV3Schema:
        description: VultrBareMetalMachine is the Schema for the vultrbaremetalmachines
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VultrBareMetalMachineSpec defines the desired state of VultrBareMetalMachine.
            properties:
              additionalTags:
                description: AdditionalTags are added to the server on top of the
                  tags the provider manages.
                items:
                  type: string
                type: array
              enableIPv6:
                description: EnableIPv6 enables IPv6 on the server. Defaults to true.
                type: boolean
              mdiskMode:
                description: MdiskMode is the RAID configuration to use. When unset
                  the Vultr default for the plan applies.
                enum:
                - raid1
                - jbod
                - none
                type: string
              osID:
                description: |-
                  OSID is the Vultr operating system id to install (e.g. 2284 for Ubuntu 24.04).
                  Kubernetes components are then installed by the bootstrap data at first boot.
                minimum: 1
                type: integer
              planID:
                description: |-
                  PlanID is the Vultr bare metal plan (e.g. vbm-4c-32gb). Availability is per region;
                  see GET /v2/plans-metal.
                minLength: 1
                type: string
              providerID:
                description: ProviderID is the unique identifier as specified by the
                  cloud provider (vultr://<server-id>).
                type: string
              region:
                description: Region is the Vultr region the server is deployed in.
                minLength: 1
                type: string
              sshKey:
                description: SSHKey is the list of Vultr SSH key ids to install on
                  the server.
                items:
                  type: string
                type: array
              vpcID:
                description: |-
                  VPCID is the id of the VPC to attach once the server is active. The plan must
                  support VPC networking.
                type: string
            required:
            - osID
            - planID
            - region
            type: object
          status:
            description: VultrBareMetalMachineStatus defines the observed state of
              VultrBareMetalMachine.
            properties:
              addresses:
                description: Addresses contains the associated node addresses.
                items:
                  description: NodeAddress contains information for the node's address.
                  properties:
                    address:
                      description: The node address.
                      type: string
                    type:
                      description: Node address type, one of Hostname, ExternalIP
                        or InternalIP.
                      type: string
                  required:
                  - address
                  - type
                  type: object
                type: array
              conditions:
                description: Conditions represent the observations of the machine's
                  current state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 32
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              cpu:
                description: CPU is the number of CPUs of the server.
                type: integer
              disk:
                description: Disk is the disk configuration as reported by Vultr (e.g.
                  "2x 240GB SSD").
                type: string
              initialization:
                description: |-
                  Initialization provides observations of the machine initialization process.
                  NOTE: Fields in this struct are part of the Cluster API contract and are used
                  to orchestrate initial machine provisioning.
                  The value of these fields is never updated after provisioning is completed.
                minProperties: 1
                properties:
                  provisioned:
                    description: |-
                      Provisioned is true when the infrastructure provider reports that the machine's
                      infrastructure is fully provisioned.
                      NOTE: this field is part of the Cluster API contract, and it is used to orchestrate initial Machine provisioning.
                    type: boolean
                type: object
              ram:
                description: RAM is the amount of memory as reported by Vultr (e.g.
                  "32768 MB").
                type: string
              ready:
                description: Ready indicates the infrastructure is ready to be used.
                type: boolean
              subscriptionStatus:
                description: SubscriptionStatus represents the status of the Vultr
                  subscription.
                type: string
              vpc:
                description: VPC describes the VPC attachment, if any.
                properties:
                  id:
                    description: ID is the VPC id.
                    type: string
                  ipAddress:
                    description: IPAddress is the address of the server inside the
                      VPC.
                    type: string
                  macAddress:
                    description: MACAddress is the MAC address of the VPC interface.
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
