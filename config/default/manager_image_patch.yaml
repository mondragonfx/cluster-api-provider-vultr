apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      # Change the value of image field below to your controller image URL
      - image: sjc.vultrcr.com/dragoncity/cluster-api-provider-vultr:v1beta2-2
        name: manager
