apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Cluster and addons only; the ClusterClass is published separately from ./clusterclass.
resources:
- ../../addons/helm
- ../../addons/vultr-ccm
- cluster-template.yaml
